import React from 'react';
import MovielistContainer from '../container/MovielistContainer'

const Home = () => {
    return (
        <div>
            <MovielistContainer />
        </div>
    );
};

export default Home;